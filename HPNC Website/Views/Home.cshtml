@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@using Archetype.Models;
@using HPNC_Website.Models;

@{
    Layout = "WebBase.cshtml";
}

<div class="container hpnc-tile-container">
    @foreach (var fieldset in Model.Content.GetPropertyValue<ArchetypeModel>("featuredContent"))
    {

        var imageSrc = customURLHelper.UmbracoFile(Umbraco.Media(fieldset.GetValue("image")).Url.ToString());
        var linkId = @fieldset.GetValue("page");
        var link = Umbraco.Content(linkId);


        <div class="col-xs-12 col-sm-6 col-md-4">
              <figure class="effect-honey hpnc-tile" >
                <img src="@imageSrc" alt="img01" class="home-featured-content" />
                <figcaption class="home-title">
                    <h2 class="home-title-header"> <a href="@fieldset.GetValue("link")" title="" class="home-title-link">@fieldset.GetValue("name")</a></h2>
                </figcaption>
            </figure>
        </div>
    }
</div>
